@model HealthCare.Models.Empresa

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@using (Html.BeginForm("Login", "Empresas"))
{
    @Html.AntiForgeryToken()

    <div class="form-horizontal" style="padding: 5% 10%">
        <h2>Login</h2>
        <br />
        <h4>Empresas</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        <div class="form-group">
            @Html.LabelFor(model => model.IDEmpresa, htmlAttributes: new { @class = "control-label col-md-2 req" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.IDEmpresa, new { htmlAttributes = new { @class = "form-control", @id = "login", @style = "width: 40%" } })
                <label hidden="hidden" class="mierror" id="errorLabel">@ViewBag.error</label>
            </div>
        </div>
        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Entrar" class="btn btn-success" />
            </div>
        </div>
    </div>
}

<script>
    var errorLabel = $("#errorLabel");
    if (errorLabel.text().length > 0) {
        errorLabel.attr("hidden", null);
    } else {
        errorLabel.attr("hidden", "hidden");
    }
</script>
