@using HealthCare.Models
@{
    
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container" style="margin-top:20px">
    <table class="table">
        <thead>            
            <tr class="success" style="border-bottom-width:1px; border-bottom-color:#dbdbdb; border-bottom-style: solid">
                <td>#</td>
                <td>Nombre</td>
                <td>Precio</td>
            </tr>
        </thead>
        @if (Model.Count > 0)
        {
            @Html.Partial("PartialItems", Model as List<Item>);
        }
        else
        {
            <tbody>
                <tr><td><br /></td></tr>
                <tr>
                    <td colspan="3">Esta empresa no tiene productos que registrados aun</td>
                </tr>
            </tbody>
        }
    </table>
</div>
